apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vm-app
  namespace: openshift-gitops
spec:
  destination:
    server: https://api.cluster3.alflab.att.com:6443
    namespace: default
  project: vm-app-project
  source:
    path: vms
    repoURL: https://gitlab.alflab.att.com:7443/mabe/ztp-pipeline.git
    targetRevision: main
    # uncomment the below plugin if you will be adding the plugin binaries in the same repo->dir where
    # the sitconfig.yaml exist AND use the ../../hack/patch-argocd-dev.sh script to re-patch the deployment-repo-server
#    plugin:
#      name: kustomize-with-local-plugins
  syncPolicy:
    automated:
      prune: true
      allowEmpty: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
